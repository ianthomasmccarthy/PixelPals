#!/bin/bash

BASE='/opt/pixelpals/'
VENV_PYTHON='${BASE}venv/bin/python'
SCRIPT='run.py'

start() {
    cd ${BASE}
    ${VENV_PYTHON} ${BASE}${SCRIPT}
    return 0
}


case "$1" in
  start)
    start
    ;;
  *)
    echo "Usage: $0 {start}"
esac
