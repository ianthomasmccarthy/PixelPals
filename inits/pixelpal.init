#!/bin/bash

BASE='/opt/pixelpals/'
VENV_PYTHON='${BASE}venv/bin/python'
SCRIPT='single/pixel_pal_runner.py'

start() {
    ${VENV_PYTHON} ${BASE}${SCRIPT} --host=`/bin/hostname`
    return 0
}


case "$1" in
  start)
    start
    ;;
  *)
    echo "Usage: $0 {start}"
esac
